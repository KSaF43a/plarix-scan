version: '3.8'

services:
  plarix:
    build: .
    image: plarix-scan:latest
    ports:
      - "8080:8080"
    volumes:
      - ./ledgers:/data
    restart: unless-stopped
    # Optional: override upstreams or other env vars
    # environment:
    #   - PLARIX_DEBUG=true

  # Example 'app' service using the proxy
  # app:
  #   image: python:3.9
  #   environment:
  #     - OPENAI_BASE_URL=http://plarix:8080/openai
  #     - ANTHROPIC_BASE_URL=http://plarix:8080/anthropic
  #   command: python my_script.py
